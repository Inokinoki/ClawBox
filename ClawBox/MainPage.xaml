<Page
    x:Class="ClawBox.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClawBox"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Pivot x:Name="MainPivot" SelectionChanged="MainPivot_SelectionChanged">
            <PivotItem Header="Server">
                <Grid Padding="24" RowSpacing="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Spacing="8">
                        <TextBlock x:Name="TitleText" Text="ClawBox" FontSize="28" FontWeight="SemiBold"/>
                        <TextBlock x:Name="SubtitleText" Text="OpenClaw + ChakraCore JS runtime embedded for Xbox / UWP" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" FontSize="14"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="12">
                        <Button x:Name="StartButton" Content="Start server" Click="StartButton_Click" MinWidth="120"/>
                        <Button x:Name="StopButton" Content="Stop server" Click="StopButton_Click" MinWidth="120"/>
                    </StackPanel>

                    <TextBlock Grid.Row="2" x:Name="StatusText" Text="Status: Stopped" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" TextWrapping="Wrap"/>

                    <StackPanel Grid.Row="3" Spacing="8">
                        <TextBlock Text="Log" FontWeight="SemiBold" FontSize="14"/>
                        <ScrollViewer x:Name="LogScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" MaxHeight="140" Padding="8" Background="{ThemeResource SystemControlBackgroundAltHighBrush}">
                            <TextBlock x:Name="LogOutput" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap"/>
                        </ScrollViewer>
                    </StackPanel>

                    <StackPanel Grid.Row="4" Spacing="8">
                        <TextBlock Text="Server log" FontWeight="SemiBold" FontSize="14"/>
                        <ScrollViewer x:Name="ServerLogScrollViewer" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" MaxHeight="140" Padding="8" Background="{ThemeResource SystemControlBackgroundAltHighBrush}">
                            <TextBlock x:Name="ServerLogOutput" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap"/>
                        </ScrollViewer>
                    </StackPanel>
                </Grid>
            </PivotItem>

            <PivotItem Header="Dashboard">
                <Grid Padding="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8" Margin="0,0,0,8">
                        <Button x:Name="RefreshDashboardButton" Content="Refresh" Click="RefreshDashboardButton_Click" MinWidth="80"/>
                        <TextBlock x:Name="DashboardHint" VerticalAlignment="Center" Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}" FontSize="13" Text="In-app view of http://127.0.0.1:18789/ (start server first)"/>
                    </StackPanel>
                    <WebView x:Name="DashboardWebView" Grid.Row="1" />
                </Grid>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
